# helm/

Committed, secret-free Helm values used by the deployment. Contains:

- `cert-manager-values.yaml` — values for the cert-manager Helm release.
- `nextcloud-values.yaml` — values for the Nextcloud Helm release (version 9.1.0).

Apply order and upgrade command are in the root README.
